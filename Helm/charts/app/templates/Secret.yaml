---
apiVersion: v1
kind: Secret
metadata:
  name: app-creds
  labels:
    {{- include "app.labels" . | nindent 4 }}
stringData:
  DB_USER: "{{ .Values.db_config.db_user }}"
  DB_PASSWORD: "{{ .Values.db_config.db_password }}"
